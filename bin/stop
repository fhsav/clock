#!/bin/bash

if [ -s /var/www/fhsclock/tmp/pids/unicorn.pid ]; then
	kill -INT `cat /var/www/fhsclock/tmp/pids/unicorn.pid`
	echo -e '\E[37;44m'"\033[1mThe clock has stopped.\033[0m"
else
	echo -e '\E[37;44m'"\033[1mIt appears the clock has not started.\033[0m"
fi
